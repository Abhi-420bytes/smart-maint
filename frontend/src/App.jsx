import React from 'react';
import Dashboard from './components/Dashboard';

// Dashboard already contains its own full layout (sidebar + main content + background)
function App() {
  return <Dashboard />;
}

export default App;
